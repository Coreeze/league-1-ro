import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  image: {
    width: "100%",
    height: 200,
    borderRadius: 15,
    marginTop: 10,
    overflow: "hidden",
    resizeMode: "cover",
  },
});

export default styles;
