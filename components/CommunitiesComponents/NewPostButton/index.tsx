import React from "react";
import { TouchableOpacity } from "react-native";

const NewPostButton = () => {
  const onPress = () => {
    console.warn("Open new post page");
  };

  return <TouchableOpacity></TouchableOpacity>;
};

export default NewPostButton;
